# Codex作業ルール

## 基本方針

- パスはすべて `tennis-app/` を起点として扱う。
- 既存ファイル名を依頼なく変更しない。
- 現物のコード、ログ、スクリーンショットに基づいて調査・修正する。
- 不明点がある場合は推測で変更せず、判断に必要な現物をユーザーへ要求する。
- 業務ロジックを依頼なく変更しない。
- unrelatedな変更を同じ作業へ混ぜない。
- マイグレーションは必要な場合のみ作成する。

## Git運用

- 作業前に `main` と `origin/main` をfast-forwardで同期する。
- `main` 上では直接編集しない。
- 作業ごとに `agent/` で始まる専用ブランチを作成する。
- 想定外の変更がないことを確認してから、対象ファイルだけをステージする。
- `report.md` と `.pr-body.md` はローカル専用とし、Git管理しない。
- PR作成後は停止し、レビュー承認を待つ。
- マージはユーザーが「レビュー承認」と明示した場合のみ実行する。
- マージ時にforce push、rebase、reset、作業ブランチ削除を行わない。

## 実装と確認

- リファクタリングでは計算式、辞書キー、DB保存項目、`update_fields`、外部仕様を維持する。
- 実装後に `git diff`、`git diff --check`、対象言語の構文確認、利用可能なテストを実行する。
- テストを実行できない場合は、実行不能の原因と不足している依存関係を正確に記録する。
- 問題がなければユーザーへの途中確認なしで、以下まで進める。
  1. `git add`
  2. `git commit`
  3. `git push`
  4. ドラフトPR作成
  5. ローカル `report.md` 作成

## 途中停止する条件

次の場合のみ途中で停止し、状況と必要な判断をユーザーへ伝える。

- 想定外のファイル変更が見つかった。
- テストが失敗した。
- 業務仕様の判断が必要になった。
- `main` 上で変更が見つかった。
- force push、rebase、reset、ファイル削除、本番操作が必要になった。
- 認証または権限エラーが発生した。
