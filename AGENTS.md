# Tennis-App Codex作業ルール

## 基本方針

- パスはすべてtennis-appを起点として扱う。
- 既存ファイル名を依頼なく変更しない。
- 入力は長文、ログ、箇条書き、コード、スクリーンショット説明を含め、最後まで1つの改善要求として扱う。
- 現物のコード、ログ、スクリーンショット、既存テスト、Git履歴に基づいて調査・修正する。
- 不明点があっても、まず現物から調査できる範囲を調査する。本当に実装不能な場合のみ質問する。
- 業務ロジックを依頼なく変更しない。
- unrelatedな変更を同じ作業へ混ぜない。
- マイグレーションは必要な場合のみ作成する。
- モンキーパッチ、import副作用による動的差し替え、暫定回避を新規導入しない。

## Git運用

- 作業前にmainとorigin/mainをfast-forwardで同期する。
- main上では直接編集しない。
- 作業ごとにagent/で始まる専用ブランチを作成する。
- ブランチ名は作業内容を短く表す英小文字のkebab-caseにする。
- 想定外の変更がないことを確認し、対象ファイルだけを明示的にステージする。
- report.mdと.pr-body.mdはローカル専用とし、Git管理しない。
- PR作成後は停止し、レビュー承認を待つ。Codex自身はPRをマージしない。
- マージはレビュー承認後にscripts/merge-pr.ps1からのみ実行する。
- force push、rebase、reset、作業ブランチ削除を行わない。

## 実装と確認

- リファクタリングでは計算式、辞書キー、DB保存項目、update_fields、公開インターフェース、画面仕様を維持する。
- 必要なテストを追加または修正し、利用可能な関連テストを実行する。
- 実装後にgit diff、git diff --check、git diff --stat、変更ファイル一覧を確認する。
- PowerShell変更時は全変更スクリプトをPowerShell ASTで構文確認する。
- Python変更時は対象ファイルへpython -m py_compileを実行する。
- 既存業務コードへの影響を確認してreport.mdへ記録する。
- テストを実行できない場合は、コマンド、エラー、依存不足などの理由を正確に記録する。
- 問題がなければ途中確認なしで、対象ファイルのgit add、commit、push、Draft PR作成、ローカルreport.md作成まで進める。
- .pr-body.mdはDraft PR作成後に削除する。

## 途中停止する条件

次の場合のみ停止し、状況と必要な判断を伝える。

- テストが失敗した。
- 業務仕様の判断が必要になった。
- main上で変更が見つかった。
- force push、rebase、reset、ブランチ削除、PRマージ、本番操作が必要になった。
- 認証または権限エラーが発生した。

## 自動化スクリプト

- 通常開発はscripts/new-task.ps1、用途別実行はscripts/codex-auto.ps1を入口にする。
- 共通処理はscripts/codex-common.ps1へ集約し、各入口へ重複実装しない。
- 改善内容の入力待ちは1回だけとし、「改善内容を入力してください」と表示する。
- 入力全体を1つの要求として扱い、途中で続きの有無を確認しない。
- prompt-base.txtと用途別prompt-*.txtを合成してCodexへ渡す。
- report.mdと.pr-body.mdをステージまたはコミットしない。
- scripts/merge-pr.ps1以外からPRマージを実行しない。

## report.md必須項目

- 変更概要
- 変更ファイル
- 設計意図
- 挙動変更の有無
- 自動化される内容
- 操作手順
- 従来との差分
- 追加・変更したスクリプト
- テンプレート一覧
- 自動化しない危険操作
- 実行した確認
- 未実行確認と理由
- PR番号
- PR URL
- 最新コミットSHA
- 残リスク
